import React, { Component } from 'react'

import Base64 from '../components/Base64';

export class Base64Page extends Component {

    render() {
        return (
            <div className="page">
                <Base64/>
            </div>
        )
    }
}

export default Base64Page
